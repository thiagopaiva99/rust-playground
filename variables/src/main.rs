fn main() {
    let x = 45; // -> immutable data
    let mut y = 60; // -> mutable data // DATA

    let a: i64 = 1; // -> data types
    let b: f32 = 6.5;
    let c: bool = true;

    println!("The value of x is {}", x);
    println!("The value of y is {}", y);
}
